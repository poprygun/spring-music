platform: linux

image_resource:
  type: docker-image
  source:
    repository: maven
    tag: latest

inputs:
  - name: resource-music-repo

outputs:
  - name: output

run:
  path: sh
  args:
    - resource-music-repo/ci/tasks/maven-package.sh
